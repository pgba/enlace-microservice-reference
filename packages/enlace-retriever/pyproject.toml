[project]
name = "enlace-retriever"
version = "0.1.0"
description = "Retriever service role abstractions for Enlace microservices"
requires-python = ">=3.12"
dependencies = [
    "enlace-contracts",
    "enlace-core",
    "pydantic>=2.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/enlace_retriever"]

[tool.uv.sources]
enlace-core = { workspace = true }
enlace-contracts = { workspace = true }
